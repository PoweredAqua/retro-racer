This template has:

- A working Garage system that reads from an autoload
- A Dealership system that reads from a .csv
- A credits system that prevents the player from buying a vehicle if they do not have enough credits
- Two debug buttons to add a car direct to garage, and a button that adds credits. These integers are changable in the files.
- A working main menu that can be navigated, and a working quit button.